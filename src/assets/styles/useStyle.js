import { makeStyles } from '@mui/styles'

export const useStyle = makeStyles({
    link: {
        textDecoration: 'none',
        color: 'blue'
    },
    linkNormal: {
        textDecoration: 'none',
        color: "black"
    }
})